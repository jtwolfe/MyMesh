# Changelog

## 0.1.0-alpha.3
- Labels, aliases, groups + `hosts` / `resolve`
- Magic plane on `serve`: DNS (*.mym), SOCKS5, 127.64 mesh IPs, auto port forwards
- SSH ProxyCommand (`ssh-config`, `proxy-ssh`)
- `expose` TCP tunnel helper
- Connect-by-carrier phone page (no phone node)
- Reconnect probes + systemd Restart=always
- Known: Hyprland fullscreen TUI chrome still imperfect

## 0.1.0-alpha.2
- Mesh gossip, kicks, TUI, install lifecycle, file browser, shell
