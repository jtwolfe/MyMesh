[package]
name = "mymesh-desktop"
description = "Remote desktop capture and input injection (platform backends)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
default = []
# Enable real capture backends when building on a desktop OS with deps present.
capture = []

[dependencies]
mymesh-core = { workspace = true }
mymesh-protocol = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
